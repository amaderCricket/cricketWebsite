[build]
  command = "npm run build"  # Command to run your build (adjust if needed)
  

[build.environment]
  NODE_VERSION = "22.15.0"       # Optional: specify the Node.js version you want to use

# Optional: Specify install settings
[build.processing]
  install = true
